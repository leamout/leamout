// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: number_orders.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const createNumberOrder = `-- name: CreateNumberOrder :one
INSERT INTO number_orders (
    organization_id,
    provider_id,
    provider_inventory_id,
    provider_product_id,
    number,
    country_code
)
SELECT
    $1,
    cp.id,
    $2,
    $3,
    $4,
    $5
FROM organizations AS o
JOIN carrier_providers AS cp
  ON cp.id = $6
 AND cp.status = 'active'
WHERE o.id = $1
  AND o.status = 'active'
  AND o.deleted_at IS NULL
RETURNING number_orders.id, number_orders.organization_id, number_orders.provider_id, number_orders.provider_inventory_id, number_orders.provider_product_id, number_orders.number, number_orders.country_code, number_orders.status, number_orders.phone_number_id, number_orders.error_code, number_orders.error_message, number_orders.created_at, number_orders.updated_at
`

type CreateNumberOrderParams struct {
	OrganizationID      uuid.UUID `db:"organization_id" json:"organization_id"`
	ProviderInventoryID string    `db:"provider_inventory_id" json:"provider_inventory_id"`
	ProviderProductID   string    `db:"provider_product_id" json:"provider_product_id"`
	Number              string    `db:"number" json:"number"`
	CountryCode         string    `db:"country_code" json:"country_code"`
	ProviderID          uuid.UUID `db:"provider_id" json:"provider_id"`
}

func (q *Queries) CreateNumberOrder(ctx context.Context, arg CreateNumberOrderParams) (NumberOrder, error) {
	row := q.db.QueryRow(ctx, createNumberOrder,
		arg.OrganizationID,
		arg.ProviderInventoryID,
		arg.ProviderProductID,
		arg.Number,
		arg.CountryCode,
		arg.ProviderID,
	)
	var i NumberOrder
	err := row.Scan(
		&i.ID,
		&i.OrganizationID,
		&i.ProviderID,
		&i.ProviderInventoryID,
		&i.ProviderProductID,
		&i.Number,
		&i.CountryCode,
		&i.Status,
		&i.PhoneNumberID,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getNumberOrderByID = `-- name: GetNumberOrderByID :one
SELECT no.id, no.organization_id, no.provider_id, no.provider_inventory_id, no.provider_product_id, no.number, no.country_code, no.status, no.phone_number_id, no.error_code, no.error_message, no.created_at, no.updated_at
FROM number_orders AS no
JOIN organizations AS o ON o.id = no.organization_id
WHERE no.id = $1
  AND no.organization_id = $2
  AND o.status = 'active'
  AND o.deleted_at IS NULL
LIMIT 1
`

type GetNumberOrderByIDParams struct {
	ID             uuid.UUID `db:"id" json:"id"`
	OrganizationID uuid.UUID `db:"organization_id" json:"organization_id"`
}

func (q *Queries) GetNumberOrderByID(ctx context.Context, arg GetNumberOrderByIDParams) (NumberOrder, error) {
	row := q.db.QueryRow(ctx, getNumberOrderByID, arg.ID, arg.OrganizationID)
	var i NumberOrder
	err := row.Scan(
		&i.ID,
		&i.OrganizationID,
		&i.ProviderID,
		&i.ProviderInventoryID,
		&i.ProviderProductID,
		&i.Number,
		&i.CountryCode,
		&i.Status,
		&i.PhoneNumberID,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listNumberOrdersByOrganizationID = `-- name: ListNumberOrdersByOrganizationID :many
SELECT no.id, no.organization_id, no.provider_id, no.provider_inventory_id, no.provider_product_id, no.number, no.country_code, no.status, no.phone_number_id, no.error_code, no.error_message, no.created_at, no.updated_at
FROM number_orders AS no
WHERE no.organization_id = $1
ORDER BY no.created_at DESC
`

func (q *Queries) ListNumberOrdersByOrganizationID(ctx context.Context, organizationID uuid.UUID) ([]NumberOrder, error) {
	rows, err := q.db.Query(ctx, listNumberOrdersByOrganizationID, organizationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []NumberOrder{}
	for rows.Next() {
		var i NumberOrder
		if err := rows.Scan(
			&i.ID,
			&i.OrganizationID,
			&i.ProviderID,
			&i.ProviderInventoryID,
			&i.ProviderProductID,
			&i.Number,
			&i.CountryCode,
			&i.Status,
			&i.PhoneNumberID,
			&i.ErrorCode,
			&i.ErrorMessage,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
